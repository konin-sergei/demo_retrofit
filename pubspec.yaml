name: retrofit_fake_api
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  retrofit: ^4.0.1
  dio: ^5.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^7.0.1
  build_runner: ^2.4.5
  json_serializable: ^6.7.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

